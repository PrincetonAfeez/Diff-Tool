# Runtime dependencies
# ----------------------
# None. The library and CLI use only the Python standard library.
#
# Install (editable, recommended for development):
#   pip install -e .
#
# Install (non-editable):
#   pip install .
#
# Development tools (pytest, coverage, ruff, mypy) are optional extras
# declared in pyproject.toml. Use one of:
#   pip install -e .
#   pip install -r requirements-dev.lock
#   pip install -r requirements-dev.txt
